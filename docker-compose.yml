version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:80"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./backend/nourish.db:/app/backend/nourish.db
    restart: unless-stopped

